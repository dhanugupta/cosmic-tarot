export class CardInterpretation {
  card: string;
  interpretation: string;

  constructor(card: string, interpretation: string) {
    this.card = card;
    this.interpretation = interpretation;
  }
}
